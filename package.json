{
  "dependencies": {
    "express": "^4.18.2",
    "hbs": "^4.2.0",
    "mongoose": "^7.6.3",
    "nodemon": "^3.0.1"
  },
  "name": "logintutorial",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node -e 'let port = 3004; require(\"./src/index\").listen(port, () => console.log(`Server is running on port ${port}`))' || node -e 'let port = 4001; require(\"./src/index\").listen(port, () => console.log(`Server is running on port ${port}`))' || node -e 'let port = 4002; require(\"./src/index\").listen(port, () => console.log(`Server is running on port ${port}`))'"
  },
   
  "author": "",
  "license": "ISC",
  "description": ""
}
